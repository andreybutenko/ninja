<% page.posts.reverse().each(function(post) { %>
  <%- partial('../ribbon', {post: post, index: true}) %>
<% }) %>

<div class="ribbon ribbon-blank">
  <div class="ribbon-body thin">

  </div>
</div>
